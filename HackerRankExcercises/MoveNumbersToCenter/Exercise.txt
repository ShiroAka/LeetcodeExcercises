Given an array 'arr' with integer numbers and a number 'num', move all appearances of 'num' to the center of the array.

Hint: To find the center, you can use the formula: center = floor(len(arr) / 2))